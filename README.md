# AI-Finlab

It's a project to develop AI-powered financial analysis API using RAG (Retrieval-Augmented Generation) techniques.

## Running

To run the project:

1. Clone the repository;
2. Install dependencies using `uv sync`;
3. Enter the api directory;
4. Start the server with `uvicorn main:app --reload`.
